==Coding Standards==

* Do not use strptime(). Use DateTime::createFromFormat() instead so
  as to support Windows.

* When using $_SERVER['HTTPS'], remember that HTTPS being disabled may
  be signalled either by the key being unset (everything) or the value
  being 'off' (IIS).
